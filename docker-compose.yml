version: '3.8'

services:
  vinylbuddy:
    image: potseeslc/vinylbuddy:latest
    container_name: vinylbuddy
    ports:
      - "3000:3000"
    volumes:
      - vinylbuddy_uploads:/app/uploads
    environment:
      - MUSICBRAINZ_CONTACT=REMOVED_CONTACT_EMAIL
      # - ACOUSTID_API_KEY=your-acoustid-api-key  # Uncomment and set if you have an API key
      # - MAX_UPLOAD_MB=25  # Optional: Set maximum upload size in MB
      - LASTFM_API_KEY=REMOVED_LASTFM_API_KEY  # Uncomment and set for Last.fm scrobbling
      - LASTFM_SECRET=REMOVED_LASTFM_SECRET  # Uncomment and set for Last.fm scrobbling
      - LASTFM_USERNAME=potseeslc  # Uncomment and set for Last.fm scrobbling
      - LASTFM_SESSION_KEY=REMOVED_LASTFM_SESSION_KEY  # Uncomment and set for Last.fm scrobbling
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    labels:
      - "com.centurylinklabs.watchtower.enable=true"  # For auto-updates if using Watchtower

volumes:
  vinylbuddy_uploads:
    driver: local
